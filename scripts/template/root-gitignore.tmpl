# personal local-only zone (see docs/agents/git-workflow.md: never pushed, never PR'd)
.scratch/
# secrets: setup copies .env.example files (never moves them), real keys stay local
.env
.sandcastle/.env
